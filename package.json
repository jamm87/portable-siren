{
  "name": "portable-siren",
  "version": "1.0.0",
  "description": "Portable Dub Siren — a dub siren synthesizer (Web Audio) shipped as a PWA, an Android app and an iOS app via Capacitor.",
  "private": true,
  "scripts": {
    "sync": "cap sync",
    "sync:android": "cap sync android",
    "sync:ios": "cap sync ios",
    "build:apk": "cap sync android && cd android && ./gradlew assembleDebug",
    "build:aab": "cap sync android && cd android && ./gradlew bundleRelease"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jamm87/portable-siren.git"
  },
  "keywords": ["dub", "siren", "synthesizer", "web-audio", "capacitor", "pwa"],
  "author": "jamm87",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/jamm87/portable-siren/issues"
  },
  "homepage": "https://github.com/jamm87/portable-siren#readme",
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
